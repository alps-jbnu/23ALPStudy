#include <iostream>
using namespace std;

int t, v, e;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> t;
	while (t--)
	{
		cin >> v >> e;
		while (e--)
		{
			int a, b;
			cin >> a >> b;
		}
		cout << v - 1 << "\n";
	}
	return 0;
}
